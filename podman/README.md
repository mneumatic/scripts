# Podman

These scripts have been made for `RPM` and `DEB` based Linux systems (i.e Fedora | Ubuntu)

`open_ports` function uses `firewalld` & `ufw` repectively. Scripts are not coded to handle `iptables`. You will need to do this manually if you choose to use `iptables`.